# Document/package checks — revision 1.2

**Performed 6 September 2026. These are not application tests.**

Checked: the supplied revision-1.1 ZIP opens without CRC errors; baseline content and the 1.1 amendment remain byte-for-byte preserved; superseded root guidance/checksums are moved under HISTORY; the four new/updated Markdown entry documents decode as UTF-8 and have balanced fenced blocks; required reading-order targets exist; the 36 CE acceptance IDs are unique; archive entry names have no absolute or parent-traversal paths; the new ZIP opens without CRC errors and every listed SHA-256 digest matches the packaged bytes.

Not checked by execution: application behavior, domain/type schemas for the new contracts, repository integration, generated UI, browser interactions, model/provider/source calls, sandbox safety, privacy enforcement, course-generation accuracy, code builds, CI gates, release/rollback, actual user review, or educational effectiveness. No such implementation exists in this amendment. The acceptance scenarios are unexecuted specifications.

The old JSON Schemas and historical validation report were retained, not upgraded or revalidated for revisions 1.1/1.2. Document integrity is not proof of architectural correctness.
