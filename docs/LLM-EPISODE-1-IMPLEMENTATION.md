# Episode 1 implementation contract

**Superseded story scope — 15 September 2026:** the workshop below describes the implemented prototype at `31025192a4c01dee04fcd621c1e4a52887b4efcb`. The user requested story revision. The next bounded implementation follows [LLM-RESCUE-STORY.md](LLM-RESCUE-STORY.md) and [WORLD-ATMOSPHERE-AND-AUDIO.md](WORLD-ATMOSPHERE-AND-AUDIO.md): arrival/capture/repair/reunion, followed by changed-context transfer. Preserve the existing learning safeguards and receipts; version changed content/rules/assessment fixtures rather than reinterpret saved attempts. Do not build later episodes before the Phase 1 human checkpoint.

15 September 2026. User authorized beginning after GitHub research, with the reusable framework actually used. See REUSE-RESEARCH-20260915.md and NEXT-TEACHING-DESIGN.md.

## Bounded complete path

Serve a separate /word-machine entry in the same local/hosted application, preserving the existing retry route and history. The player enters a 3D message workshop, operates a tiny next-piece machine, witnesses a wrong destination, adds a missing clue, regenerates the message one piece at a time and sends the robot to the intended door. A second context challenge changes the target and distractor so the first answer cannot simply be copied. The chapter ends with a visible return/reunion and an honest preview of the next episode.

Use the existing GameRuntime, PlayCanvas backend, WorldSpec validation, player controls and procedural player. Add reusable workshop/token-track props and a spec-world adapter, not another renderer. Author compact portrait staging. One current goal, one contextual action region and optional inspection/series details. Text pieces are the manipulated language objects; exposition does not obscure the world.

## Storage and learning boundary

New immutable content, activity, competency and frame identities; no migration/relabeling of retry evidence. Reuse existing commands/receipts/revisions and attempts. Preserve the one-active-draft rule; explain an existing other-course draft rather than overwrite it. An append-only bounded semantic action log replays through GameRulesEngine. Server derives context, output and success; client animation cannot certify a result. Simulation feedback remains assisted practice; completion is not mastery. A second fresh layout within guided practice is not certified independent transfer.

## Validation before expanding

First verify legal/illegal rule transitions, context-dependent output, append-only history, stale/conflicting commands, restart/resume, learner isolation and immutable submissions. Then build the world and manually play fresh entry, wrong output, repair, second challenge, replay and controls-after-save on desktop and 360/390/430. Inspect reduced motion and keyboard semantics. Use separate temporary databases for automated checks. Keep the existing build/test/browser gates and critic policy; stop at the user's Phase 1 review checkpoint. No live model API or production deploy in this increment.

## Realized opening and reusable boundary

The existing `openGameOpening` controller presents one spatial arrival/wake scene. The courier brings the flower to the sleeping machine; a semantic wake action reveals the illuminated core and courier response. Skip, pause and isolated replay remain available. A completed action marker can disappear so it does not cover the revealed object. The same GameRuntime proceeds into gameplay. Phone-specific recenter distance is an optional validated shared player-profile field.

The machine/track/pavilion/tree/flower/book props are procedural WorldSpec components. Required models must finish loading before entry; engine/module/model failures expose a reload path. The renderer does not decide whether an experiment passed. See REUSABLE-ASSETS.md for ownership and reuse proof.
