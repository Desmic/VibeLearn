# Consolidated Astra review — 20 September 2026

Candidate: `7856b054c3f3671764c32e6cb7da2ff14d363daa`, local main, not deployed.
Build and 356 application tests passed; seven PostgreSQL tests skipped locally.
Exact verification and retained recording manifests are under
`artifacts/readiness-20260920/candidate-7856b05/`.

One Astra reviewer began with no inherited conversation, saved cold recording and
GUI reports before reading intent, then judged every lane including art/world.
It completed opening, separate tutorial, Level 1, deliberate error/recovery,
pre-feedback reload, relay, distinct ending and completion reload. Five native
screenshots were retained. Saved first errors survived later success; mastery
remains unknown. The reviewer inspected saved evidence but did not recompute it.

Verdict: **needs revision / incomplete required evidence**. Cold story, cinematic
causality, art/world and intent comparison rated 7; tutorial, chapter and bounded
learning rated 8. Motion, audio and physicality were unassessed. Ratings are not
averaged into readiness. The full unchanged review is at
`artifacts/readiness-20260920/candidate-7856b05/astra-review/LANE-REVIEW.md`.

Material findings:
- The stolen chest object reads as a power core; speech identity comes from captions.
- The town's ritual is readable, but purposeful life and attachment are thin.
- Panel actions dominate; the panel hides the body and route boards face poorly.
- Collision/contact, continuous animation, listening and full device coverage need
  appropriate observation. Sampled recording frames cannot certify these.

The recording had a brief opening caption leak. The reviewer never saw that sheet
and inspected 3.2–23.4 seconds at five frames per second. The missing beginning
limits attachment conclusions; do not assert that no initial relationship cue exists.

## Next bounded repair

Start at the earliest unresolved gate: speech-object causality in the opening.
Use parameterized speech marks attached to speakers and existing timeline cues:
an exchange while the same chest module is attached, then an empty failed speech
attempt after extraction. Keep reduced-motion outcomes and replay cleanup correct.
Observe the actual result through GUI and retain recordings. This is a proposed
repair, not a declaration that the cold readability issue is solved.

Later address purposeful world behavior and embodied play against these findings.
No Level 2, deployment promotion or live Terminal PM/API dispatch is authorized.

## Opening repair implemented after the review

The working tree adds a parameterized speech-bubble prop and uses existing timeline
patches for Zip/Mira's exchange, a visible link from the attached module, and a
crossed empty bubble after extraction. Marks clear between scenes and on handoff.
This changes presentation only; learning rules and saved evidence are untouched.

The implementation author actively played all eight beats and the lantern action
through the tutorial handoff at 1280x720, checking the theft/handoff at 390x844.
The normal and failed marks were visible and cleared on handoff. This informed
recheck is not a new cold verdict and does not raise the original critic scores.
Continuous animation and listening remain unassessed.

Build and all 356 application tests pass (seven skipped); ten focused framework
checks also pass. The opening browser group passed with rendered checks
for the successful and failed communication marks, replay/save preservation and
360/430/desktop reduced-motion paths. Retained recordings and changed-source hashes
are in `artifacts/readiness-20260920/speech-repair/verification.json`. Its earlier attempt exposed a
review-interface mismatch in the new test (corrected), not a player failure.

Recording preparation now suppresses captions using a same-origin stylesheet in
the intercepted test document before first paint. This keeps production CSP intact
and removes the earlier after-navigation caption leak. No production document or
security setting is changed by the test fixture. Initial inline-style preparation
was rejected by CSP and the hidden-caption assertion caught it; that attempt was
not used as evidence.

## Follow-up: shared pose stability and phone composition

The same Astra critic's recheck could not acquire a browser, even after the parent
opened a disposable tab. It retained this as a tool failure and inspected only the
prior screenshots. It found improved speech/loss cues, but head crowding and a
clipped friend balloon. See `speech-repair/CRITIC-RECHECK.md`; no cold score changed.
The package now places smaller marks above faces and brings the friend's mark
inward. Parent native replay at 390x844 observed both marks within the viewport
and Zip's face clear. Parent keyboard input also advanced movement practice and
returned the character to a normal resting pose. This is informed native evidence.

An unrelated actor fixture reproduced two backend defects: holding component speed
at zero freezes transition progress, and sparse imported clips leave non-keyed
joints at their prior gesture/run transforms. The backend now assigns speed per
clip, preserving the blend clock, and adds constant bind-pose channels for omitted
transforms using existing PlayCanvas animation types. Original imported assets and
tracks remain unchanged. No character or story IDs enter the shared fix.

`tests.animation_rest_browser` verifies a real nonzero gesture, completed
transitions and identical skeleton transforms after both gesture and run. It
compares equivalent quaternion rotations correctly and runs in the active
foundation group. Original failure is retained in
`artifacts/readiness-20260920/animation-rest-before.json`; corrected report is
`artifacts/animation-rest-report.json`. This does not certify animation artistry.

After these repairs, build, 356 application tests (seven skips), the opening
browser group and the synthetic pose regression passed. Retained recordings and
source hashes: `artifacts/readiness-20260920/pose-repair/verification.json`.

## Next bounded slice: purposeful background activity

Replace the two workers' aimless patrol with a visible flower delivery behind the
friend group. Reuse the existing delivery prop and timeline moves/cues: the courier
approaches, the pot changes hands at a shared position, the recipient brings it
closer and the courier leaves. Keep the main lantern interaction dominant. Reset
all positions/visibility on replay; reduced motion must show the delivered outcome.
The normal activity must stop at the rupture, leaving an empty square. This is a
concrete repair attempt, not an assertion that world-life criticism now passes.


Delivery is implemented with the existing two workers, flower prop and timeline
moves/cues. Both workers occupy a separate background row; the courier leaves the
central composition after handoff. Rewinding restores individually hidden actors,
and a completed lantern action also restores the already-delivered pot.

Native phone/desktop resizing exposed another shared defect: the package adapter
restored the gameplay player position while fitting an authored cinematic shot.
It now leaves story shots to the cinematic camera and only fits the gameplay view
in mission mode. Zip remained visible during actual same-scene phone→desktop
review. An automated check waits two rendered frames after each resize.

Build, all 356 application tests (seven skipped), the opening browser group and
the focused settled-resize check pass. Native parent play observed the pot on the
courier before delivery, on the recipient afterward, the empty disrupted square,
and rewind. These are informed observations, not continuous-motion certification.
Bound media/source hashes are in `artifacts/readiness-20260920/town-repair/verification.json`.
The sole critic also tried the documented iab entry point; its session still had
no browser. No new independent score or acceptance is claimed. Required art/world,
physical play, full motion and audio gates remain open.
