# Supervised native repair and independent return review

Controlled local diagnostic, not production acceptance or external dispatch.
Artifacts: `artifacts/readiness-20260920/`.

An Astra-operated source session retained its real initial screenshot and stopped
deliberately before acting. Coverage correctly reported missing action/after capture.
The existing proposal builder created a bounded child request from a fixture parent.
No Terminal PM transport or paid-provider dispatch was invoked.

A fresh tab/session replay acquired a new initial screenshot, received one guarded
click permit, clicked the visible Move right control, and acquired the resulting
screenshot. The visible player moved from Start to Key; inventory became collected
and the gate label OPEN. Two captures, one successful game input, zero final gaps.
Evidence-inbox paste/submit actions and tab setup were separate from game inputs.
The source and fresh initial screenshots have equal hashes because their visible
states are identical; the fresh tool capture was actually performed.

`qualify_native_repair` sealed the return. A fresh, non-forked Luna reviewer saw
only the assignment/capsule and actual images. It passed coverage, but initially
misidentified the resulting player tile as Path. Root challenged that specific
observation; Luna reinspected and corrected it to Key. Preserve this silent miss:
the pass is audited/assisted, not evidence that uncertainty self-signals suffice.

The harness receipt records that follow-up context. Qualification returned
`evidence_repair_qualified`, `product_acceptance=undetermined` and no dispatch
authority. Reviewer correctly noted that the capsule records click type but not
target identity, and cannot authenticate live acquisition. The supervisor's separate
dispatch journal retains Move right and the permit acknowledgement. Capture origin,
manual acknowledgements and checkpoint semantics still trust the supervisor;
shared filesystem access is not process-level isolation. This demonstrates the
local supervised repair/review boundary, not a generally qualified autonomous executor.


Independent boundary audit reproduced a whitespace identity bypass: comparing raw
reviewer identities before receipt normalization could admit a self-review.
Qualification now compares validated reviewer identities against canonical worker
bindings; fresh-session comparison is canonical too. Four adversarial regressions
and 68 focused tests pass. The existing direct self-review test now uses a valid
receipt so rejection actually proves independence enforcement. Requalification
of the retained real run produces the identical result; no captures or reviews
were rewritten. The original finding and independent recheck are retained in
`artifacts/readiness-20260920/system-boundary-review.md`.
